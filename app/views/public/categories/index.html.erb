<div class="container">
  <div class="row" style="margin-top: 70px;">
    <% @categories.each do |category| %>
    <div class="col-6 mt-5" style="height: 75px; text-align: center;">
      <%= link_to category.name, category_path(category.id), class: "btn btn-primary w-100", style: "line-height: 75px;" %>
    </div>
    <% end %>
  </div>
</div>
